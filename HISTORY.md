# HISTORY

## 2026-03-06 (인증 화면 고도화 + 회원가입 안내 모달 도입)
- 변경 목적: 로그인 화면의 완성도를 높이고, 회원가입 전환 시 사용자 혼란을 줄이기 위해 가입 안내 흐름(새 창/현재 창 선택 + 이메일 인증 안내)을 명확히 제공
- 사용자 체감 변화: 인증 화면이 쇼케이스+폼 구조로 정리되어 첫인상이 개선되고, `회원가입으로 전환` 클릭 시 안내 모달에서 가입 경로를 선택할 수 있으며 인증 완료 후 로그인 단계가 분명해짐
- 남은 리스크/다음 작업: 가입 후 메일 인증 완료 여부를 UI에서 자동 감지하지는 않으므로, 필요 시 인증 상태 폴링/재확인 UX를 추가 검토

## 2026-03-06 (AI 요약 분량 제한 상향)
- 변경 목적: 요약이 지나치게 짧게 생성되는 체감을 줄이기 위해 AI 입력/출력 토큰 예산과 길이 지시문을 확장
- 사용자 체감 변화: `짧게/보통/길게` 모두 기존 대비 더 긴 요약이 생성되고, 특히 `길게`는 핵심 수치/맥락을 포함한 장문 요약이 더 안정적으로 출력됨
- 남은 리스크/다음 작업: 요약 길이가 늘면서 응답 시간이 증가할 수 있어, 필요 시 모델별 타임아웃/토큰 상한 프로파일 분리 검토

## 2026-03-06 (다중선택 모드 분리 + 휴지통 영구삭제 + 업로드 중 이탈 방지)
- 변경 목적: 기본 카드 클릭 편집 흐름을 유지하면서 다중 작업은 명시적 모드에서만 수행되도록 정리하고, 휴지통 데이터 정리와 업로드 안정성을 보강
- 사용자 체감 변화: `다중선택`을 켜야 체크박스가 나타나고 카드 클릭이 선택 동작으로 바뀌며, 휴지통에서는 단건/일괄 `영구삭제`가 가능해짐. 파일 가져오기 중에는 새로고침/이탈이 차단되어 중단 사고가 줄어듦
- 남은 리스크/다음 작업: 브라우저별 `beforeunload` 경고 문구 표시는 제한될 수 있어, 장기적으로 서버측 import job + 복구 가능한 재시도 UX 도입 검토

## 2026-03-06 (import AI 자동실행 토글 + 재조회 깜박임 완화)
- 변경 목적: 파일 업로드 후 AI가 자동 실행되는 동작을 제어 가능하게 하고, AI 처리/재조회 시 화면 깜박임을 줄이기 위한 로딩/조회 전략 개선
- 사용자 체감 변화: 설정에서 `가져오기 후 AI 자동 분석`을 켜고 끌 수 있으며(기본 꺼짐), 배치 AI 처리 중에는 무음(silent) 재조회로 전체 화면 로딩 깜박임이 크게 줄어듦
- 남은 리스크/다음 작업: 대량 배치 시에는 여전히 목록 데이터 갱신 리렌더가 있으므로, 필요 시 카드 단위 부분 업데이트(optimistic patch)로 추가 최적화 가능

## 2026-03-06 (브랜드/내비/밀도 재정렬 2차)
- 변경 목적: 브랜드 표기 오류(`LinkLens`)와 불필요한 상단 보조 버튼/헤더 텍스트를 제거하고, 좌측 내비 숫자 표시와 전체 레이아웃 비율을 더 깔끔하게 재균형
- 사용자 체감 변화: 좌상단 로고가 `LinkPocket`으로 복구되고, 링크 영역의 `링크 목록` 텍스트/상단 `초기화·새로고침` 버튼이 제거됨. 라이브러리 진입 버튼이 좌측으로 복귀하며 좌측 숫자는 박스 없이 텍스트만 표시되어 밀도가 정돈됨
- 남은 리스크/다음 작업: 상단 버튼 축소로 빠른 초기화 동작은 사라졌으므로, 필요 시 설정 탭에 필터 리셋 단축 동작을 보강 가능

## 2026-03-06 (설정 진입 위치 조정 + 보안/AI 기능 보강 + 버튼 톤 개선)
- 변경 목적: 설정 접근 동선을 사용자 메뉴(로그아웃 영역)로 통합하고, 설정 내 실사용 기능(미분류 AI/비밀번호 변경)을 강화하며 전체 버튼 인상을 더 각진 고급 톤으로 보정
- 사용자 체감 변화: 사용자 메뉴에서 `설정`으로 진입 가능, 설정 탭에서 `미분류 전체 AI 분석`과 `비밀번호 변경`을 바로 수행 가능. 링크 목록 상단 `n개` 배지는 제거되고 버튼/칩이 덜 둥근 스타일로 정리됨
- 남은 리스크/다음 작업: 비밀번호 변경은 현재 세션 기준 즉시 반영되므로, 필요 시 변경 후 재인증 안내 UX를 추가 검토

## 2026-03-06 (설정 탭 신설: 가져오기/내보내기/테마/글자크기/전체삭제)
- 변경 목적: 상단에 흩어진 환경·데이터 기능을 한 곳에서 관리하고, 업로드에 대응하는 내보내기 및 전체 삭제(확인 후 실행) 기능을 제공
- 사용자 체감 변화: 사이드바 `설정` 탭에서 파일 가져오기(CSV/JSONL/JSON), JSONL/CSV 내보내기, 다크/라이트 변경, 글자 크기 조절, 기사 전체 삭제를 처리할 수 있음
- 남은 리스크/다음 작업: 전체 삭제는 영구 삭제 방식이므로, 필요 시 2차 확인(입력형)이나 백업 자동 생성 옵션 추가 검토

## 2026-03-06 (import 날짜 보정: CSV 지원 + published_at 저장/표시)
- 변경 목적: 업로드 기사의 날짜가 업로드 시점으로만 보이던 문제를 해결하고, 실제 제공 파일 포맷(CSV/JSONL)의 날짜/키워드 필드를 정확히 반영
- 사용자 체감 변화: import 시 `date_iso/date_raw/date`를 파싱해 `published_at`에 저장하고, 카드/상세 날짜는 `published_at` 우선으로 표시됨. CSV 파일도 바로 업로드 가능
- 남은 리스크/다음 작업: `date_raw`가 연도 없는 `M.D` 형식일 때는 파일 내 다수 연도(없으면 현재 연도)로 보정하므로, 특이 케이스는 수동 확인 필요

## 2026-03-06 (다크 톤 재정렬 + 카테고리 카운트 고정 + 상세 자동저장)
- 변경 목적: 블루 편중 테마를 줄이고, 카테고리 선택 시 숫자 왜곡 문제 및 상세 편집 저장 버튼 의존을 해소
- 사용자 체감 변화: 전체 화면이 뉴트럴 다크 톤으로 정리되고, 카테고리 숫자는 필터와 무관하게 전체 기준으로 안정 표시됨. 상세 패널은 변경 즉시 자동 저장되어 별도 `변경 저장` 버튼 없이 반영됨
- 남은 리스크/다음 작업: 자동 저장은 입력 중 네트워크 지연 시 `저장 중...` 상태가 길어질 수 있어, 필요 시 필드별 저장 실패 표시를 추가 검토

## 2026-03-06 (태그 누적형 AI + 카테고리 7개 축약 + 컬렉션 자동색상)
- 변경 목적: AI 분석 시 기존 태그 체감이 사라지는 문제를 줄이고, 과도한 카테고리 수/상세창 URL 가독성/컬렉션 색상 입력 번거로움을 동시에 개선
- 사용자 체감 변화: AI 분석 후 키워드가 기존 값에 누적(중복 제거)되고, 상세 편집창의 긴 URL은 `원문 열기` 버튼으로 정리됨. 카테고리는 7개 한글 체계로 축약되며, 컬렉션 생성 시 색상이 자동 배정됨
- 남은 리스크/다음 작업: 기존 DB에 희귀 카테고리 문자열이 매우 다양하면 `기타`로 모이므로, 원하면 후속 SQL로 카테고리 컬럼 일괄 정규화 가능

## 2026-03-06 (카테고리 체계 슬림화)
- 변경 목적: 카테고리 수가 과도해 선택/필터링 부담이 커진 문제를 줄이기 위해 분류 체계를 축약
- 사용자 체감 변화: 카테고리가 14개에서 9개(`AI/개발, 데이터/인프라, 비즈니스/경제, 투자/금융, 과학/헬스, 사회/정책, 교육/커리어, 라이프/문화, 기타`)로 단순화되어 탐색이 쉬워짐
- 남은 리스크/다음 작업: 기존 데이터의 오래된 카테고리 값은 화면/필터에서 호환 매핑으로 처리 중이며, 필요 시 DB 일괄 마이그레이션으로 완전 정규화 가능

## 2026-03-06 (미분류 전체 AI 일괄 실행 + 상세 편집창 토글형 개편)
- 변경 목적: 업로드된 미분류 기사에 대해 한 번에 AI 분류를 돌리고, 상세 편집창의 입력 동선을 버튼/토글 중심으로 단순화
- 사용자 체감 변화: 상단 `미분류 전체 AI` 버튼으로 카테고리 없는 링크를 일괄 분석할 수 있고, 상세창에서 상태/별점은 작은 토글 버튼으로 빠르게 조작되며 메모 입력 영역이 넓어져 편집성이 향상됨
- 남은 리스크/다음 작업: 일괄 AI가 대량 데이터에서 오래 걸릴 수 있어 진행률 표시(n/m)와 중단 기능 추가 검토 필요

## 2026-03-06 (초보 사용자 기준 안내 규칙 반영)
- 변경 목적: 코딩 비경험 사용자도 바로 실행할 수 있도록 확인 절차 안내 수준을 더 구체화
- 사용자 체감 변화: 확인 요청 시 화면 경로/정상 예시/문제 예시/복붙 가능한 값까지 함께 받아 시행착오 감소
- 남은 리스크/다음 작업: 실제 안내 문구가 길어질 수 있어 핵심 우선 + 상세 보조 형식 유지 필요

## 2026-03-06 (사용자 협업 성향 지침 누적 반영)
- 변경 목적: 세션/채팅이 바뀌어도 동일한 소통 품질을 유지하도록, 실제 대화에서 확인된 사용자 협업 선호를 README 지침에 명시
- 사용자 체감 변화: 다음 작업자도 결과 우선/짧은 실행 보고/자동 커밋푸시 선호를 동일하게 따라 반복 설명 비용 감소
- 남은 리스크/다음 작업: 성향은 변할 수 있으므로 주기적으로 문구를 최신화해야 함

## 2026-03-06 (좁은 가로폭 버튼 줄바꿈/긴 URL 카드 넘침 보정)
- 변경 목적: 창 폭이 줄어들 때 상단 버튼 텍스트가 줄바뀌고, AI 처리 전 긴 URL 제목이 카드 바깥으로 넘치는 UI 깨짐 수정
- 사용자 체감 변화: 상단 액션 버튼이 줄바꿈 없이 안정적으로 배치되고, 긴 링크/제목/요약이 카드 내부에서 안전하게 줄바꿈되어 레이아웃 깨짐 감소
- 남은 리스크/다음 작업: 극단적으로 좁은 폭에서는 액션 개수가 많아 상단 줄 수가 늘어날 수 있어 버튼 우선순위 축약 UX 검토 필요

## 2026-03-06 (AI 401 완화: 최신 세션 토큰 재조회)
- 변경 목적: AI 제목/요약 호출 시 간헐적인 `401 Unauthorized`를 줄이기 위해 요청 직전에 최신 세션 토큰을 재조회하도록 보강
- 사용자 체감 변화: 토큰 만료/동기화 타이밍 이슈에서 AI 분석 실패 빈도 감소, 401 발생 시 사용자 메시지 명확화
- 남은 리스크/다음 작업: Worker의 `SUPABASE_URL/ANON_KEY`가 웹과 다른 프로젝트를 가리키면 401은 계속 발생하므로 운영 환경값 일치 점검 필요

## 2026-03-06 (AI 호출 경로 복구 + 미구현 도움말 버튼 제거)
- 변경 목적: `VITE_API_BASE_URL` 오설정 시 AI 제목/요약 요청이 Pages 상대경로로 빠져 실패하던 문제를 줄이고, 기능 없는 `?` 버튼으로 인한 혼동 제거
- 사용자 체감 변화: 프로덕션에서 API 경로가 비정상일 때 Worker 기본 주소로 자동 복구되어 AI 기능 실패 확률 감소, 상단 UI에서 의미 없는 버튼 제거
- 남은 리스크/다음 작업: Worker 자체 환경변수(`OPENAI_API_KEY` 등) 누락 시에는 `/api/v1/health` 확인이 여전히 필요

## 2026-03-06 (Pages 빌드 실패 타입 정합성 수정)
- 변경 목적: Pages 빌드에서 `published_at` 필드 타입 누락으로 `App.tsx` 타입체크가 실패하던 오류 수정
- 사용자 체감 변화: `npm run build`가 정상 통과되어 최신 UI 배포가 막히지 않음
- 남은 리스크/다음 작업: 배포 파이프라인에서 타입 변경 누락 재발 방지를 위해 빌드 전 타입체크 점검 루틴 유지 필요

## 2026-03-06 (UI 전면 리모델링: 각진 하이엔드 톤으로 재정렬)
- 변경 목적: 기존 둥근 카드 중심 톤에서 벗어나, 정보 밀도와 신뢰감을 높이는 메이저 서비스 스타일로 전면 재디자인
- 사용자 체감 변화: 사이드바/탑바/카드/모달/상세패널이 날카로운 라인과 고대비 타이포 중심으로 정리되어 화면 완성도와 스캔 속도 향상
- 남은 리스크/다음 작업: 현재 카운트 집계는 클라이언트 count 호출 기반이므로 대량 데이터 환경에서 서버 집계 RPC로 최적화 검토 필요

## 2026-03-06 (라이브러리 카운트 고정 집계 + 스크롤바 뷰포트 적용)
- 변경 목적: 상태/검색/카테고리 필터에 따라 라이브러리 숫자가 흔들리는 문제와 브라우저 기본(흰색) 스크롤바 잔존 문제를 제거
- 사용자 체감 변화: 사이드바/상단 통계 숫자가 현재 필터와 무관하게 라이브러리 전체 기준으로 유지되고, 시크릿 창 포함 뷰포트 스크롤바가 테마 색으로 통일됨
- 남은 리스크/다음 작업: Supabase count 쿼리가 늘어 집계 호출 수가 증가하므로 대량 데이터 환경에서는 서버 집계 RPC로 최적화 검토 필요

## 2026-03-06 (재발 방지 소통 지침 업데이트)
- 변경 목적: 같은 대화 실수가 다른 세션에서 반복되지 않도록, 기술 절차가 아닌 소통 방식 중심 규칙으로 정리
- 사용자 체감 변화: 답변이 의도 확인 -> 사실 전달 -> 바로 확인 가능한 액션 순서로 정돈되어 커뮤니케이션 마찰 감소
- 남은 리스크/다음 작업: 소통 규칙이 실제 세션에서 일관되게 지켜지는지 다음 작업부터 지속 점검 필요

## 2026-03-06 (라이브러리 카운트/스크롤바 보정)
- 변경 목적: 상태 필터 클릭 시 `전체 기사` 카운트가 함께 변하는 혼동과 브라우저 흰색 스크롤바 이질감을 수정
- 사용자 체감 변화: 상태 버튼은 목록만 필터링하고 라이브러리 카운트는 일관되게 유지됨, 스크롤바가 앱 테마 색상으로 통일됨
- 남은 리스크/다음 작업: 상태 외 필터(카테고리/컬렉션/즐겨찾기)에서 카운트 기준을 전역 고정할지 정책 확정 필요

## 2026-03-05 (import 직후 백그라운드 AI 자동 보강 강화)
- 변경 목적: 파일 가져오기 후 수동 재분석 없이도 기사 제목/요약/카테고리가 자동 채워지도록 import 후 AI 큐를 자동 실행
- 사용자 체감 변화: import 완료 직후 `AI 보강 시작` 상태로 백그라운드 작업이 진행되고, 완료 시 일괄 완료 토스트를 받음. URL이 없는 import 행도 메모/제목 기반 분석으로 요약/카테고리 자동 보강 가능
- 남은 리스크/다음 작업: 대량 import 시 처리 진행률 UI(예: n/m)와 중간 취소 기능 추가 필요

## 2026-03-05 (import 시 태그/키워드 분리 저장)
- 변경 목적: 파일 가져오기에서 `keywords`가 태그와 키워드 컬럼에 중복 반영되어 구분이 흐려지는 문제 수정
- 사용자 체감 변화: import 데이터의 `keywords/tags`는 태그로만 저장되고, 키워드 컬럼은 AI 분석 결과 전용으로 유지되어 역할이 명확해짐
- 남은 리스크/다음 작업: 기존에 중복 저장된 과거 데이터는 일괄 정리 스크립트(키워드→태그 마이그레이션) 제공 필요

## 2026-03-05 (JSONL/JSON 파일 가져오기 기능 추가)
- 변경 목적: 대표님 엑셀 기반 기사 관리 전환을 위해 웹에서 파일 업로드 후 기사 데이터를 일괄 등록하는 import 기능 추가
- 사용자 체감 변화: 상단 `파일 가져오기` 버튼으로 JSONL/JSON 파일을 바로 업로드해 다건 등록 가능. URL이 없는 행은 검색 링크를 자동 생성해 누락 없이 적재하고, URL이 있는 행은 AI 자동 분석까지 이어짐
- 남은 리스크/다음 작업: 현재는 JSONL/JSON 중심이므로 `.xlsx/.csv` 직접 파싱 UI와 컬럼 매핑 화면(미리보기) 추가 필요

## 2026-03-05 (AI 카테고리 자동 분류 세분화)
- 변경 목적: 카테고리 자동 배정 체감이 약한 문제를 해결하기 위해 Worker 분류 체계를 세분화하고, 저장 모달 기본값을 `자동 분류(AI)`로 전환
- 사용자 체감 변화: URL 저장 후 AI 분석 시 `AI/머신러닝, 개발/프로그래밍, 투자/금융 ...` 등 세분화 카테고리로 자동 배정되며, 사이드바 카테고리 필터가 해당 값을 즉시 표시/검색 가능
- 남은 리스크/다음 작업: 카테고리 정확도는 콘텐츠 품질에 따라 변동 가능하므로, 실제 사용 로그 기반으로 카테고리 사전과 매핑 규칙 지속 보정 필요

## 2026-03-05 (대시보드 정보구조 재정렬: 컬렉션 관리 사이드바 이전)
- 변경 목적: 본문 대시보드의 집중도를 높이기 위해 컬렉션 생성/관리 영역을 사이드바로 이동하고, 본문의 중복 필터 패널을 제거
- 사용자 체감 변화: 대시보드는 통계/툴바/목록 중심으로 단순화되고, 컬렉션은 사이드바에서 생성·수정·삭제·필터링이 한 번에 처리되어 탐색 흐름이 자연스러워짐
- 남은 리스크/다음 작업: 컬렉션 개수 집계는 현재 로드된 목록 기준이므로 대용량/복합필터 환경에서 서버 집계 방식으로 개선 여지 있음

## 2026-03-05 (인수인계 문서 기반 UX 1차 반영)
- 변경 목적: 리마스터링 인수인계 문서 기준으로 사용자 체감이 큰 항목(테마 토글/카테고리 네비/모달 제목 자동완성)을 우선 구현
- 사용자 체감 변화: 사이드바 테마 버튼이 다크/라이트를 실제 전환하며 상태를 기억하고, 카테고리 고정 메뉴 클릭으로 즉시 필터링 가능. 링크 추가 모달에서 URL 입력 600ms 후 제목이 자동완성됨
- 남은 리스크/다음 작업: 컬렉션 인라인 수정/삭제 UI, 유저 드롭다운, 휴지통 로컬 정책 고도화는 후속 작업 필요

## 2026-03-05 (대시보드 카드 뷰를 아이콘 탐색기 스타일로 개선)
- 변경 목적: 링크 목록의 시인성을 높이기 위해 기존 텍스트 밀집 카드에서 폴더 탐색기형 아이콘 카드 레이아웃으로 전환
- 사용자 체감 변화: 카드에서 아이콘/제목/상태를 빠르게 스캔할 수 있고, 액션 버튼은 하단 고정되어 대량 링크 탐색과 조작 속도 개선
- 남은 리스크/다음 작업: 실제 데이터 기준 카드 정보 밀도(요약 길이/태그 노출)와 모바일 카드 터치 영역 추가 튜닝 필요

## 2026-03-05 (링크 추가 모달 전환)
- 변경 목적: 대시보드 레이아웃은 유지하고, 기존 인라인 링크 입력 섹션을 모달 기반 입력 UX로 교체
- 사용자 체감 변화: `+ 링크 추가` 클릭 시 중앙 모달에서 URL/제목/카테고리/상태/태그/메모를 빠르게 입력하고 저장할 수 있으며, 배경 화면은 유지된 채 오버레이로 집중도 향상
- 남은 리스크/다음 작업: 제목 자동감지(미리보기) UX와 저장 전/후 상태 표시를 모달 기준으로 추가 고도화 필요

## 2026-03-05 (전 프로젝트 UI 네비 구조 추가 정렬)
- 변경 목적: 전 프로젝트 화면과의 체감 차이를 줄이기 위해 사이드바 네비 흐름(`나중에 읽기`, `즐겨찾기`)을 실제 동작 필터와 함께 복원
- 사용자 체감 변화: 라이브러리 탐색에서 `즐겨찾기` 전용 뷰를 즉시 열 수 있고, 기존 `읽음` 항목이 `나중에 읽기` 표현으로 맞춰져 익숙한 정보 구조로 사용 가능
- 남은 리스크/다음 작업: 카테고리 고정 메뉴/도움말 패널 등 레퍼런스 세부 인터랙션은 추가 구현 필요

## 2026-03-05 (LinkLens 기준 대시보드 UI 전면 개편)
- 변경 목적: 기존 화면의 부분 수정 수준을 넘어서, 판매 가능한 완성도를 목표로 LinkLens 레퍼런스 구조(사이드바 260, 헤더 컨트롤, 요약카드 4종, 칩 필터, 뷰 토글, 빈 상태)를 전면 반영
- 사용자 체감 변화: 첫 화면에서 정보 구조가 명확해지고(라이브러리/필터/핵심지표), `+ 링크 추가` 진입이 빨라졌으며, 검색/정렬/미읽음 필터/그리드-리스트 전환이 한 줄 컨트롤에서 즉시 동작
- 남은 리스크/다음 작업: 실제 사용자 데이터 기준으로 카드 밀도와 모바일 대응 최종 튜닝, 도움말 버튼 실기능 연결 필요

## 2026-03-05 (로그아웃 불능 수정 + UI 전면 톤 리디자인)
- 변경 목적: 로그아웃 버튼 비동작 이슈를 우선 복구하고, 기존 대비 체감되는 수준으로 시각 톤/레이아웃을 전면 재조정
- 사용자 체감 변화: 로그아웃 클릭 시 즉시 로컬 세션/화면 상태가 정리되어 인증 화면으로 빠르게 전환되고, 전체 UI가 네이비-민트 톤의 대시보드 스타일로 갱신되어 카드/패널 가독성과 조작 피드백이 강화됨
- 남은 리스크/다음 작업: 실제 계정으로 `로그아웃 -> 즉시 재로그인` 회귀 테스트와 모바일 화면 최종 손질 필요

## 2026-03-05 (안정성 우선 리팩터링 + 카드 UI 업그레이드)
- 변경 목적: 저장/AI/인증 흐름의 주요 실패 지점을 줄이고, 링크 카드 조작 UI를 빠르고 직관적으로 개선
- 사용자 체감 변화: 저장 요청 타임아웃 처리로 무한 로딩 체감이 줄고, AI 분석은 일시 오류 시 1회 자동 재시도로 성공률이 높아짐. 카드 액션이 아이콘+툴팁으로 정리되어 스캔/조작 속도 향상
- 남은 리스크/다음 작업: 인증 레이스/저장 무한 로딩/재로그인 이슈는 배포 환경 실사용 시나리오(E2E)로 재현 테스트 후 최종 확정 필요

## 2026-03-05 (전 버전 문서 선별 반영)
- 변경 목적: 전 버전 정리 문서에서 사용자 편의/안정성에 직접 기여하는 항목만 선별해 현재 백로그/이슈 문서에 반영
- 사용자 체감 변화: 구현 우선순위가 시각 개선 중심에서 `저장 무한 로딩 방지`, `인증 레이스 정리`, `중복 가입 안내` 같은 실제 장애 예방 중심으로 정렬됨
- 남은 리스크/다음 작업: `KNOWN_ISSUES.md`에 기록한 3개 항목은 아직 미재현 상태이므로 로컬/배포 환경에서 재현 후 사실 이슈로 확정 필요

## 2026-03-05 (Cloudflare 실연결 완료)
- 변경 목적: Pages/Workers와 Supabase/OpenAI 키를 실계정 기준으로 연결해 배포 환경에서 실제 동작 가능한 상태로 전환
- 사용자 체감 변화: `linkpocket.pages.dev`(웹)와 `linkpocket-api.lame26.workers.dev`(API)가 모두 200 응답으로 확인되고, Worker `/api/v1/health`가 `ok:true`로 정상화됨
- 남은 리스크/다음 작업: 링크 저장 -> AI 자동분석까지 실제 사용자 플로우 E2E 검증 및 실패 재시도 정책(P0) 구현 필요

## 2026-03-05
- 변경 목적: LinkPocket MVP를 Cloudflare Pages + Workers + Supabase + OpenAI 구조로 시작하기 위한 초기 스캐폴딩 구성
- 사용자 체감 변화: 이메일 인증, 링크 저장/수정/삭제/복원, 태그/컬렉션/필터/검색/정렬, AI 제목 미리보기/분석 호출이 가능한 기본 UI와 API 골격 확보
- 남은 리스크/다음 작업: 실제 Supabase/Cloudflare 프로젝트 연결값 주입, 로컬에서 Supabase 연결 후 E2E 시나리오 검증, Pages/Workers 배포 연결

## 2026-03-05 (Worker 설정 보강)
- 변경 목적: Worker 환경변수 누락 시 원인 파악이 어려운 문제를 줄이고, Cloudflare secret 등록 절차를 명확화
- 사용자 체감 변화: `/api/v1/health`에서 누락된 필수 env 키를 즉시 확인 가능, 누락 상태에서 API 호출 시 명확한 `misconfigured_worker` 오류로 빠른 복구 가능
- 남은 리스크/다음 작업: 실제 계정 secret 주입 후 원격 Worker 배포 및 웹에서 API_BASE_URL 연결 검증 필요

## 2026-03-05 (웹 UI/UX 품질 개선)
- 변경 목적: MVP 스캐폴딩 수준의 밋밋한 화면을 실제 사용 가능한 정보 밀도와 시각적 완성도로 개선
- 사용자 체감 변화: 헤더 통계 카드, 저장/필터 섹션 설명, 링크 카드 메타(날짜/별점/컬렉션 색상/AI 상태)가 추가되어 가독성과 탐색 속도 향상
- 남은 리스크/다음 작업: 실제 사용자 데이터 기준으로 카드 액션 우선순위와 모바일 조작 동선 추가 튜닝 필요

## 2026-03-05 (LinkLens 레퍼런스 기반 비주얼 재정렬)
- 변경 목적: 기존 `linklens2.pages.dev` 수준의 대시보드 감성과 정보 구조(사이드바/탑바/다크 카드)로 UI 톤을 정렬
- 사용자 체감 변화: 전체 화면이 단순 폼 나열에서 대시보드형 레이아웃으로 전환되어, 탐색(사이드바)과 작업(탑바/카드)이 분리되고 시각적 밀도/완성도 상승
- 남은 리스크/다음 작업: 레퍼런스 대비 세부 인터랙션(모달 기반 편집, 패널 전환, 단축키)은 후속 단계에서 추가 필요

## 2026-03-05 (저장 후 AI 자동 보강 플로우 복구)
- 변경 목적: 링크 저장 시 즉시 저장 후 백그라운드에서 제목/요약/키워드/카테고리/태그가 자동 생성되도록 핵심 플로우 복구
- 사용자 체감 변화: 저장 버튼 한 번으로 링크가 바로 목록에 뜨고, 잠시 뒤 AI 분석 결과가 자동 반영되어 수동 분석 버튼 의존도 감소
- 남은 리스크/다음 작업: AI 분석 실패 재시도 정책(지수 백오프, 큐 처리)과 분석 소요시간 가시화 UI 추가 필요

## 2026-03-05 (AI 호출 네트워크 경로 안정화)
- 변경 목적: 원격 브라우저 환경(Codespaces HTTPS)에서 `localhost:8787` 직접 호출로 발생하던 `Failed to fetch` 문제 제거
- 사용자 체감 변화: AI 제목 미리보기/분석 API가 Vite `/api` 프록시를 통해 안정적으로 호출되어 버튼 동작 실패 빈도 감소
- 남은 리스크/다음 작업: 프로덕션 환경에서는 `VITE_API_BASE_URL`을 Worker 실주소로 유지하는 배포 체크 필요

## 2026-03-05 (제목/요약 자동화 강제)
- 변경 목적: 저장 폼에서 수동 제목 입력/미리보기 의존을 제거하고 저장 후 AI 자동 보강 플로우를 기본 동작으로 고정
- 사용자 체감 변화: 사용자는 URL 중심으로 빠르게 저장만 하면 되고, 제목/요약/태그/카테고리는 백그라운드에서 자동 채워짐
- 남은 리스크/다음 작업: 분석 중 상태 표기(진행률/재시도 횟수)를 카드에 더 직관적으로 노출할 필요

## 2026-03-05 (OpenAI JSON 파싱 안정화)
- 변경 목적: AI 분석 중 간헐적으로 발생하던 `Unexpected end of JSON input` 오류 제거
- 사용자 체감 변화: 모델 응답이 코드블록/부분 텍스트 형태로 와도 JSON 추출을 시도해 분석 성공률 향상
- 남은 리스크/다음 작업: 모델 응답 품질이 지속적으로 불안정할 경우 재시도 1회 정책 추가 고려

## 2026-03-05 (AI 파싱 에러 추가 방어)
- 변경 목적: OpenAI/Supabase 응답 바디가 비거나 불완전할 때 JSON 파싱 예외가 그대로 노출되는 문제 차단
- 사용자 체감 변화: 기존 `Unexpected end of JSON input` 대신 원인 구분 가능한 에러로 기록되어 디버깅 속도 향상
- 남은 리스크/다음 작업: OpenAI 분석 요청 자체 재시도(1회) 전략을 추가하면 일시적 네트워크 오류 복원력 개선 가능

## 2026-03-05 (P0 편의성 리팩터링)
- 변경 목적: 카드 내 과도한 인라인 편집을 제거하고 읽기 중심 카드 + 우측 상세 편집 패널 구조로 전환
- 사용자 체감 변화: 목록 스캔 속도 향상(제목/요약 2줄 고정), 저장/AI 진행 토스트 피드백, AI 실패 시 카드 즉시 재시도 버튼 제공
- 남은 리스크/다음 작업: 카드 액션 아이콘화, 키보드 단축키(상세 패널 열기/닫기) 추가 시 작업 속도 추가 개선 가능

## 2026-03-05 (클릭 시 읽음 자동 전환)
- 변경 목적: 저장 직후 미읽음 링크를 열람할 때 수동 상태 변경 없이 자동으로 읽음 처리되도록 개선
- 사용자 체감 변화: 카드 클릭 순간 빨간 미읽음 점이 사라지고 상태가 `읽음`으로 전환되어 관리 부담 감소
- 남은 리스크/다음 작업: 읽음 자동 전환 기준을 카드 클릭 외 `외부 링크 실제 오픈 시점`으로 확장할지 정책 결정 필요

## 2026-03-05 (연속 작업 지침 강화)
- 변경 목적: 세션이 끊겨도 다음 작업자가 빠르게 문맥을 복구하도록 README 기반 문서 로딩 루틴과 향후 작업 목록을 명시
- 사용자 체감 변화: `README.md`만 확인해도 필수 문서(결정/이슈/다음작업/셋업/이력)로 자연스럽게 이어져 업무 연속성 향상
- 남은 리스크/다음 작업: `NEXT_TASKS.md` 상태를 실제 진행에 맞게 지속 갱신해야 효과 유지

## 2026-03-05 (오늘 마감 정리: import 후 백그라운드 AI 자동 처리 + 배포 안정화)
- 변경 목적: 가져오기(import) 완료 직후 사용자가 추가 작업을 하지 않아도 AI가 백그라운드에서 제목/요약/카테고리를 자동 보강하도록 고정
- 사용자 체감 변화: 파일 업로드 -> 목록 반영 -> AI 보강 자동 진행(완료 토스트) 흐름으로 단순화
- 구현 요약:
  - web: import 시 삽입된 링크 목록을 모아 `runAiEnrichmentInBackground(..., { silent: true })` 순차 실행
  - api: import 대체 URL(검색 URL) 감지 시에도 메모/제목 컨텍스트 기반으로 분석 수행
  - import 데이터 정책 정리: tags/keywords 혼선 방지를 위해 import 키워드는 태그로만 적재, `links.keywords`는 AI 전용으로 유지
  - 카테고리: AI 카테고리 카탈로그 확장 및 normalize 로직 추가
- 운영 이슈 및 조치:
  - 증상: `linkpocket.pages.dev` 접속 시 배경만 보임
  - 원인: 프론트 빌드 시 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 미주입으로 런타임 즉시 크래시
  - 조치: env 주입 재빌드 후 Pages 재배포 완료
- 배포 상태:
  - Worker: `linkpocket-api` 최신 배포 완료
  - Pages: production 최신 배포 ID `8a02f3d8-5407-4845-82d9-19e56bea9af1`
- 커밋:
  - `b18bbb0` Auto-run AI enrichment in background after file import
